{
  "version": "0.1",
  "type": "container",
  "meta": {
    "trigger": "cli"
  },
  "ops": [
    {
      "type": "container/run",
      "id": "run-from-cli",
      "args": {
        "cmd": "for i in `seq 1 10000000`; do echo This is log number $i and were still not done; done",
        "image": "ubuntu"
      }
    }
  ]
}
